3.21
在demo_spi_flash.c中增加float和字符数组相互转换的函数F2CA和CA2F
3.22
在mems_test.c中添加GetBMP085函数，实现获取BMP085的数字信号
在ad7705_test.c中添加GetAD7705函数，获取AD7705传感器两通道的adc值和电压值，并存储到全局变量adc1,adc2,volt1,volt2
3.23
在main.c中编写程序，完成将BMP085采集到的数字信号以及AD7705采集到的模拟信号以字符数组的形式存储到B[16]中去。每四字节存储一个数字，分别为volt1,volt2,g_tBMP085.Temp,g_tBMP085.Press
3.24
调换BMP085与SPI Flash初始化顺序，解决SPI Flash无法写入的问题。
3.26
在main.c中编写循环，实现采集数据的自动存储。每10s采集一次数据，每次数据16字节，每页存放半小时数据，共存放30天数据。

